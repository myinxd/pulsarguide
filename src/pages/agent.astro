---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('agent'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<Layout title="Agent" description="大模型使用心得 · Agent Skills 提效实践">
  <section class="page-header">
    <h1>🤖 Agent</h1>
    <p>大模型使用心得 · Agent Skills 提效实践</p>
  </section>

  <section class="post-list">
    {
      posts.length > 0 ? (
        posts.map((post) => (
          <article class="post-card">
            <a href={`/agent/posts/${post.id}`} class="post-link">
              <div class="post-meta">
                <time datetime={post.data.pubDate.toISOString()}>
                  {post.data.pubDate.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' })}
                </time>
                {post.data.updatedDate && (
                  <span class="updated">
                    · 更新于 {post.data.updatedDate.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' })}
                  </span>
                )}
                <div class="post-tags">
                  {post.data.tags.map((tag) => <span class="tag">{tag}</span>)}
                </div>
              </div>
              <h2 class="post-title">{post.data.title}</h2>
              <p class="post-desc">{post.data.description}</p>
            </a>
          </article>
        ))
      ) : (
        <div class="empty-state">
          <p>🤖 文章正在撰写中，敬请期待……</p>
        </div>
      )
    }
  </section>
</Layout>

<style>
  .page-header {
    padding: 3rem 0 2rem;
  }
  .page-header h1 {
    margin: 0;
    font-size: 1.8rem;
  }
  .page-header p {
    color: var(--text-secondary);
    margin-top: 0.5rem;
  }

  .post-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-bottom: 4rem;
  }

  .post-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1.5rem;
    transition: all 0.2s;
  }
  .post-card:hover {
    border-color: rgba(6, 182, 212, 0.3);
    background: transparent;
  }

  .post-link {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .post-meta {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-bottom: 0.5rem;
  }

  .updated {
    color: var(--text-muted);
  }

  .post-tags {
    display: flex;
    gap: 0.4rem;
  }

  .tag {
    background: rgba(124, 58, 237, 0.1);
    border: 1px solid rgba(124, 58, 237, 0.2);
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    font-size: 0.75rem;
    color: var(--accent-1);
  }

  .post-title {
    margin: 0 0 0.5rem;
    font-size: 1.2rem;
    color: var(--text-primary);
    transition: color 0.2s;
  }
  .post-card:hover .post-title {
    color: var(--accent-2);
  }

  .post-desc {
    color: var(--text-secondary);
    font-size: 0.9rem;
    margin: 0;
    line-height: 1.5;
  }

  .empty-state {
    text-align: center;
    padding: 4rem 0;
    color: var(--text-muted);
  }
</style>
